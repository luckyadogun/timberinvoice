version: "3"

services:
  timberr:
    build:
      context: .
    ports:
      - "8000:8000"
    volumes:
      - ./timberr:/timberr
    command: >
      sh -c "python manage.py runserver 0.0.0.0:8000"